/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["www.google.com", "images.pexels.com", "firebasestorage.googleapis.com"]
    }
};

export default nextConfig;
